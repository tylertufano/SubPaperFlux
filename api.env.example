# SubPaperFlux API — Local Dev Example

# Database: use SQLite file for local testing. For Postgres, set a proper URL like:
# postgresql://user:pass@localhost:5432/subpaperflux
DATABASE_URL=sqlite:///./dev.db

# CORS: allow the Next.js dev server origin
CORS_ALLOW_ORIGINS=http://localhost:3000
CORS_ALLOW_CREDENTIALS=1

# Secrets encryption (32-byte urlsafe base64). Generate with:
#  python - <<'PY'\nimport os, base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())\nPY
#CREDENTIALS_ENC_KEY=REPLACE_ME

# OIDC (dev placeholders). Replace with real values for actual auth.
OIDC_ISSUER=http://localhost/oidc
OIDC_CLIENT_ID=local
OIDC_CLIENT_SECRET=local
#OIDC_AUDIENCE=subpaperflux
#OIDC_USERINFO_ENDPOINT=https://idp.example.com/realms/main/protocol/openid-connect/userinfo

# Optional: create tables automatically in dev (migrations recommended)
#SQLMODEL_CREATE_ALL=1

