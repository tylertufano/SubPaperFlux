# SubPaperFlux Web — Local Dev Example

# Point UI at the local API
API_BASE=http://localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000

# NextAuth (dev placeholders). Replace for real OIDC sign-in.
NEXTAUTH_SECRET=devsecret
OIDC_ISSUER=http://localhost/oidc
OIDC_CLIENT_ID=local
OIDC_CLIENT_SECRET=local
# OIDC display name claim override (optional; defaults to heuristics)
# OIDC_DISPLAY_NAME_CLAIM=name

# CSRF header value (used if API is cookie-auth; safe no-op for bearer)
NEXT_PUBLIC_CSRF_TOKEN=1

